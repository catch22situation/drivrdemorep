apply plugin: "war"

bootWar {
    mainClassName = "com.catch22.drivrdemo.DrivrdemoApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
